some_list = [
]

[w for w in range(0,9)]

[ for w in range(0, )]

names = ["yamada", "takeuchi", "suzuki"]

result = [some_function(w) for w in some_list]


result = []
for w in some_list:
    function_result = add_function(w)
    result.append(function_result)

jp_name_list = []
for name in name_list:
    if is_jp_name(name):
        jp_name_list.append(name)



jp_name_list = [for name in name_list if is_jp_name(name)]

for key, value in some_dict.items():
    if key == 
